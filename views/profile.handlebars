<div class="row">
  <div class="page-header text-center">
      <h1><span class="fa fa-anchor"></span> Profile Page</h1>
      <a href="/logout" class="btn btn-default btn-sm">Logout</a>
  </div>

  <div class="card col s12 m6">
    <div class="card-content">
      <h3><span class="fa fa-user"></span> {{user.firstName}} {{user.lastName}} (Local)</h3>
      <p>
        <strong>id</strong>: {{user.id}}<br>
        <strong>First Name</strong>: {{user.firstName}}<br>
        <strong>Last Name</strong>: {{user.lastName}}<br>
        <strong>email</strong>: {{user.local.email}}<br>
        <strong>password</strong>: {{user.local.password}}
      </p>
    </div>
  </div>

  <div class="card col s12 m6">
    <div class="card-content">
      <h3><span class="fa fa-facebook"></span> {{user.firstName}} {{user.lastName}} (Facebook)</h3>
      <p>
        <strong>Facebook id</strong>: {{user.facebook.id}}<br>
        <strong>First Name</strong>: {{user.firstName}}<br>
        <strong>Last Name</strong>: {{user.lastName}}<br>
        <strong>Facebook email</strong>: {{user.facebook.email}}<br>
        <strong>Facebook Name</strong>: {{user.facebook.name}}
      </p>
    </div>
  </div>

</div>
